# Changelog — {DATE}

(Add site-change entries for this snapshot. Each correction day gets its
own `CHANGELOG-YYYY-MM-DD.md` in this folder; `snapshot_site.py` copies
existing changelogs forward when it creates a new dated snapshot and drops
this starter when no prior snapshot exists.)

## Corrections shipped on this date

- (date) — (commit) — (what + verification evidence)
